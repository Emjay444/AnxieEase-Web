// Dashboard entry point
// Uses the original DashboardNew.jsx with proper name field structure
// Cleaned up unused dashboard variants (DashboardNew_fixed, DashboardOld, etc.)

import Dashboard from "./DashboardNew";

export default Dashboard;
